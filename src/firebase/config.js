// 此文件已遷移到 src/lib/firebase.ts
// 請更新您的導入路徑：
// 舊: import { auth, db } from '@/firebase/config'
// 新: import { auth, db } from '@/lib/firebase'

import { auth, db } from '../lib/firebase'

export { auth, db }
